{
  "name": "lsa-heat-intelligence",
  "version": "1.0.0",
  "description": "L'SA — hyperlocal heat intelligence, standalone Android app (Capacitor port of the LSA_Life_Change Flask backend, zero server dependency)",
  "type": "module",
  "scripts": {
    "build:web": "vite build",
    "sync": "npm run build:web && npx cap sync android",
    "verify": "node verify-build.js",
    "open:android": "npx cap open android",
    "build:android": "npm run build:web && npx cap sync android && node verify-build.js && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor-community/text-to-speech": "5.1.0",
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/geolocation": "^6.0.0",
    "@capacitor/preferences": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0",
    "@capawesome-team/capacitor-android-foreground-service": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "typescript": "^7.0.2",
    "vite": "^5.4.14"
  }
}
